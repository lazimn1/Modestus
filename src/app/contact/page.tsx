import ContactUs from "@/components/ContactUs";

export const metadata = {
  title: "Contact Concierge",
  description: "Connect with the Modestus private concierge for styling consultations, bespoke tailoring, and custom sizing inquiries.",
};

export default function ContactPage() {
  return <ContactUs />;
}
